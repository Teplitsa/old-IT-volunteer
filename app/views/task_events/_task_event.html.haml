%li.task-item
  %li.role-item
    .event-info
      .event-info-avatar
        = image_tag(task_event.user.avatar_url(:thumb))
      .event-info-message
        = taskEventMessage(task_event)
        %small
          %time= task_event.created_at.strftime("%d %b %Y %H:%M")